const url = require('url');
const path = require('path');

module.exports = {
    createNewAccount: function(firstName, middleInitial, lastName, email, password) {
        console.log("We're here");
    }
}